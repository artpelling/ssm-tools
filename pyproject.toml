[project]
name = "ssm-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Art Pelling", email = "a.pelling@tu-berlin.de" }
]
requires-python = ">=3.13"
dependencies = [
    "pyfar>=0.7.3",
]

[project.scripts]
ssm-tools = "ssm_tools:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.workspace]
members = ["packages/datasets"]

[dependency-groups]
lint = [
    "ruff>=0.13.0",
]
